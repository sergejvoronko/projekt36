name = "projekt36"
compatibility_date = "2026-04-09"
compatibility_flags = ["nodejs_compat"]
main = "dist/_worker.js/index.js"

# Secrets set via Cloudflare dashboard (Workers → Settings → Variables)
# PRINTIFY_SHOP_ID and PRINTIFY_API_TOKEN

[assets]
directory = "dist"
binding = "ASSETS"
