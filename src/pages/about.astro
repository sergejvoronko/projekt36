---
import BaseLayout from '../layouts/BaseLayout.astro';
const title = 'About';
---
<BaseLayout title={title}>
  <div style="max-width:var(--wide-w);margin:0 auto;padding:6rem 1.5rem;font-family:var(--font-mono);color:var(--text-dim)">
    <p>// {title} — coming soon</p>
  </div>
</BaseLayout>
